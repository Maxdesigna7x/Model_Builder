[project]
name = "modelbuilder-backend"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["torch", "numpy", "Pillow"]

[tool.pytest.ini_options]
pythonpath = ["backend"]
testpaths = ["tests"]
addopts = "-q"
